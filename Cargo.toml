[package]
name = "lightracer"
version = "0.1.0"
authors = ["Greg Cowan <cowang4@yahoo.com>"]
edition = "2018"

[dependencies]
enum_dispatch = "0.3"
image = {version = "0.24", default-features = false, features = ["png"]}
serde = "1.0"
serde_derive = "1.0"

[profile.release]
debug = true
